### v1.4.4

**📝 优化：Step A 拒绝标准收紧**
* Step A 特殊拒绝判断从模糊的"不适合主题"改为明确列举四类拒绝范围：露骨色情、政治敏感、涉及儿童、自残/暴力/违禁品。
* 新增"轻微擦边、暧昧暗示、日常调侃必须放行"的明确约束，避免模型过度拒绝。
* 拒绝时 reason 固定为"内容涉及不适合主题，拒绝处理"，与 `_should_force_local_fallback` 逻辑对齐。

**🐛 修复：单段输出末尾句号未清理**
* 在 `on_decorating_result()` 中新增最终兜底：无论走哪条代码路径，均对 segments_text 执行 `_strip_single_trailing_period()`，确保单段输出的末尾句号被正确清理。

**✨ 新增：字数限制功能**
* 新增 `enable_max_process_length` 配置项（默认开启），控制是否启用字数限制。
* 新增 `max_process_length` 配置项（默认 500），纯文本字数（不含富媒体占位符）超过此值时跳过后处理，原样发送。
* 超长文本跳过时记录 INFO 日志，便于排查。

---

### v1.4.3

**📝 优化：提示词精简与增强**
* 精简重复文本：Step C 和清洗规则框架中的空行压缩说明改为引用"严格禁止"区域，避免三处重复。
* 精简清洗规则框架文本，降低信息密度，让用户自定义规则更突出。
* 新增3个 few-shot 示例：短文本无需分段、包含占位符、包含换行，覆盖中小参量模型的常见边界情况。
* 优化 reason 字段格式：给出具体格式示例（"清洗：做了什么；分段：N段"），替代模糊的"动作1；动作2"描述。
* 严格禁止区域新增：segments 拼接后必须与 clean_text 的可见文本完全一致。

---

### v1.4.2

**✨ 新增：处理所有回复配置项**
* 新增 `process_all_replies` 配置项（默认关闭），开启后会将插件工具调用等非 LLM 回复也纳入分段与清洗范围。
* 关闭时行为不变，仅处理 LLM 直接生成的回复。

---

### v1.4.1

**✨ 新增：分段末尾句号去除配置项**
* 新增 `strip_segment_trailing_period` 配置项（默认开启），控制每个分段是否不得以单个句号结尾。
* 将"分段末尾去句号"从清洗阶段迁移到分段阶段：清洗阶段保留所有句号不变，分段阶段负责在 segments 中去除末尾单个句号。
* 在分段规则和 Step D 中动态注入约束提示词（仅当开关开启时），省略号变式（。。。/。。）不受影响。
* 新增代码兜底 `_strip_single_trailing_period()`，即使 LLM 遗漏也能保证结果正确。
* 更新示例2/3，反映清洗阶段保留句号、分段阶段去除末尾句号的新设计。
* 从 `clean_prompt_template` 默认值中移除"清洗掉分段末尾的句号"规则（该行为现由分段阶段控制）。

---

### v1.4.0

**🐛 修复：最后一段消息发送延迟过高**
* 移除最后一段前缀发送后的多余延迟等待，修复最后一段（尤其是语音）发出明显偏慢的问题。
* 之前倒数第二段发送后会执行一次延迟等待，再由 AstrBot 管道发送最后一段，导致实际等待 = 延迟 + 管道处理时间，语音场景下尤为明显。
* 现在最后一段前缀发送后不再额外等待，直接交由 AstrBot 管道处理，最后一段的到达时间更自然。

---

### v1.3.9

**📝 更新：插件描述与配置提示词**
* 插件描述改为"LLM 驱动的回复分段器"，突出差异化。
* 精简冗余 hint，补充清洗、判别、过渡语等条目的说明。

---

### v1.3.8

**🛡️ 修复：保真校验相关问题**
* 复审阶段的保真校验与占位符补回改为使用重生成文本做参照，修复 URL/数字等合理变化被误判为"内容丢失"。
* `_normalize_number_token` 归一化时保留小数点和冒号，修复 `1.5` 与 `15` 假阳性。
* 保真校验失败时回退本地分段而非单段原文。

**🧹 清理：移除 `if False` 死代码与 pair_map 中的 `'<': '>'`**

---

### v1.3.7

**🛡️ 修复：JSON 解析与合并评分**
* JSON 解析失败时走本地分段回退。
* 弱标点开头段落合并评分改为减分，强标点开头保持正向加分。

**🧹 移除冗余配置项**
* 移除 `fallback_to_original_on_error`、`enable_segments_consistency_check`、`enable_retry_notice`。

**📝 优化：提示词与清洗规则**
* 移除用户消息传入，减少注意力干扰。
* 审查模板新增 `{{output_format_block}}`、`{{placeholder_rule_block}}`、`{{reject_reason_block}}`，去重清洗规则。
* 清洗指令措辞调整，明确包含"下方清洗要求提及的内容"。
* INFO 日志截断为 500 字符。

---

### v1.3.6

**🛡️ 修复：配置与代码默认值不一致**
* `judge_prompt_template` 和 `retry_prompt_template` 配置默认值对齐代码默认。
* `_compose_output_format_block()` 移除已声明移除的 `reason_type`。
* `_calculate_segment_delay()` 回退默认值从 `fixed` 改为 `linear`。

**🛡️ 修复：`_judge_reply()` 未传 `user_message`**

**📝 配置 hint 补充完整占位符列表**

---

### v1.3.5

**🛡️ 修复：分段后占位符仍丢失的兜底补回**
* 新增 `_final_placeholder_fallback()` 方法，在 `_normalize_segments()` 和 `_try_restore_trailing_placeholders()` 均执行完毕后做最终兜底检查。
* 对比原文与最终 segments 中的占位符数量，若仍有缺失则直接追加到最后一个非空段落末尾。
* 修复当模型的 segments 输出与 clean_text 存在微小文本差异（如标点增删）时，`_reinject_placeholders_into_segments` 逐字符匹配失败导致占位符无法从 clean_text 传递到 segments 的问题。
* 首次分段路径与重试分段路径均已接入该兜底逻辑。

---

### v1.3.4

**🛡️ 修复：clean_text 为空时 fallback 到原文**
* 在 `_process_reply()` 中新增 `clean_text` 空值检查。
* 当模型返回空 `clean_text` 时，不再继续走占位符补回逻辑导致输出纯占位符文本。
* 现在会自动 fallback 到原文，保证后处理结果始终有实际内容。

**🧷 修复：重复占位符丢失检测不准确**
* 占位符丢失检测从简单的 `len` 比较 + `in` 检查改为使用 `Counter` 精确计数。
* 修复原文存在重复占位符（如两个 `[[RP_COMP_1]]`）时，清洗后只剩一个无法被识别为丢失的问题。
* 现在会按每个占位符的出现次数逐一比对，缺失几个补回几个。

**📝 文档更新：README 与现版本逻辑对齐**
* README 删除已过时或不准确的描述，统一按当前实现说明插件行为。
* 调整对后处理上下文的说明，不再把用户消息描述成默认依赖项。
* 同步更新 `preserve_mode` 说明，使 `off / basic / strict` 的实际行为更清晰。

**🧹 优化：打回规则接入重写提示词**
* `DEFAULT_RETRY_PROMPT` 新增 `{{retry_rule_block}}` 占位。
* `_retry_generate()` 现在会实际传入 `retry_rule_block`，使打回规则在重写路径中真正生效。
* `_compose_output_format_block()` 输出格式中移除 `confidence` 字段。
* `force_local_fallback_reason` 不再暴露给用户配置。

---

### v1.3.3

**🧹 清除遗留：插件标识统一**
* 移除所有 `ReplyPolisher` 旧标识，统一改为 `PostSplitter`。
* 日志前缀：`[ReplyPolisher]` → `[PostSplitter]`
* 事件标记：`__reply_polisher_is_llm_reply` → `__post_splitter_is_llm_reply`
* 处理标记：`__reply_polisher_processed` → `__post_splitter_processed`

**📝 优化：默认模板示例增强**
* `DEFAULT_JUDGE_PROMPT` 示例新增场景说明：
  - 示例1：标注补全标点仅适用于超长连续文本
  - 示例2：明确省略号变式（三个句号）与分段末尾句号的区别处理
* `_conf_schema.json` 默认模板同步更新，与代码保持一致。

---

### v1.3.2

**📝 优化：非调试模式下的日志输出完善**
* 修改 `_log_polished_output()` 函数签名，新增 `original_text` 参数。
* 非调试模式下现在会完整输出：
  - 原文本
  - 输出完成信息（段数、总长度、模型耗时、处理模式）
  - 分段结果
* 调试模式下除上述内容外，仍会显示审查模型的完整提示词和原始输出。

**✅ 修复：分段一致性校验开关逻辑**
* 确认 `_normalize_segments()` 中的分段一致性校验逻辑已正确实现：
  - 只有开启 `enable_segments_consistency_check` 时才会比对 segments 与 clean_text
  - 开关关闭时，直接信任模型分段结果，不触发回退本地分段
  - 仅在开关开启时才会输出"segments 与 clean_text 存在可见差异"警告

---

### v1.3.1

**🔒 占位符检查永久开启**
* 在 `_process_reply()` 中新增占位符丢失检查，清洗后立即检测。
* 如果原文有 `[[RP_COMP_x]]` 占位符但清洗后丢失，自动补回到 `clean_text` 末尾，继续正常流程。
* 该检查永久开启，不受任何开关影响，确保富媒体组件不会因清洗而丢失。
* 占位符固定在文末，丢失时直接补回，不影响清洗和分段结果。

**📝 提示词动态生成**
* 新增四个方法根据开关动态生成提示词步骤：
  - `_compose_step_a_block()`：Step A（特殊拒绝判断）永远开启
  - `_compose_step_b_block()`：Step B（打回重写判断）仅当 `enable_review=true` 时传入
  - `_compose_step_c_block()`：Step C（清洗）仅当 `enable_clean=true` 时传入
  - `_compose_step_d_block()`：Step D（分段）仅当 `enable_segment=true` 时传入
* 未开启的功能不再传入对应步骤，避免扰乱模型注意力。

**📏 段数说明动态插入**
* `_compose_step_d_block()` 中动态插入段数说明，不再写死在提示词里。
* 只有开启分段功能时才会显示段数限制。

**🛡️ 加强提示词引导**
* DEFAULT_JUDGE_PROMPT 的"严格禁止"部分新增：
  - 不得删除原文中的任何内容，包括文字、符号、占位符
  - 不得遗漏原文中的任何段落或句子
  - 若正文中存在形如 [[RP_COMP_数字]] 的占位符，必须原样保留在 clean_text 中
* 强化模型对内容完整性的理解，降低漏段概率。

**🐛 修复：占位符在清洗阶段丢失后无法检测**
* 之前占位符回填机制只在分段阶段检测 `segments` 和 `clean_text` 的一致性。
* 如果模型在清洗阶段就把占位符删除，`clean_text` 和 `segments` 都没有占位符，无法触发回填。
* 现在在清洗后立即检查占位符是否丢失，丢失则自动补回 `clean_text` 末尾。

---

### v1.3.0

**🔧 配置默认值调整**
* `enable_clean` 默认值改为 `true`，清洗功能默认启用。
* `enable_segment_count_range` 默认值改为 `true`，段数限制默认启用。
* `min_segments` 默认值改为 `2`，最小段数默认为 2。
* `max_segments` 默认值仍为 `5`，最大段数保持不变。

**🧼 清洗规则新增**
* `clean_prompt_template` 默认值新增规则：`- 清洗掉分段末尾的句号`。
* 用户可根据自身语言风格需求，选择是否启用该规则。

**✂️ 分段一致性校验可配置化**
* 新增 `enable_segments_consistency_check` 配置项，默认 `false`。
* 允许用户关闭 `segments` 与 `clean_text` 的一致性校验，获得更自由的分段结果。
* 原有一致性校验过于严格，部分场景下会错误回退到本地分段，现默认关闭。

**🧹 移除未使用的配置项**
* 移除 `enable_clean_content_check` 配置项（该功能未实际启用）。
* 移除 `protected_tone_words` 配置项（该功能未实际启用）。

**📝 精简提示词与输出格式**
* `DEFAULT_JUDGE_PROMPT` 和 `_compose_output_format_block()` 去掉 `confidence` 和 `reason_type` 字段。
* 输出格式简化为：`{"action", "reason", "clean_text", "segments"}`。
* 减少模型需要填充的冗余字段，降低理解成本。

**🧪 更新示例**
* 示例1：超长文本+奇怪英文，展示"清洗非术语英文+分段"。
* 示例2：emoji+缺失标点，展示"清洗emoji+清洗掉分段末尾的句号"。

---

### v1.2.6

**🧠 优化：后处理主提示词改为严格流水线执行**
* 将默认后处理提示词明确拆分为"特殊拒绝判断 → 是否打回 → 清洗 → 分段"四步。
* 明确 `accept` 不再表示"仅判断通过"，而表示"已经完成当前已启用的后处理"。
* 降低模型只做适配性审查、不真正执行清洗与分段的概率。

**📝 优化：reason / reason_type 语义收紧**
* 强化 `reason` 的输出约束，要求在 `accept` 场景下必须描述实际处理结果，推荐使用"动作1；动作2"的格式。
* 明确禁止"符合清洗规则""文本符合要求""未发现问题"这类空洞、套话式 reason。
* 收紧 `reason_type` 语义边界，区分 `normal / persona_mismatch / weird_text / system_error / other`，提升日志可读性与可分析性。

**🧪 优化：加入短示例与一致性约束**
* 在默认提示词中加入"补全缺失标点并分段""无需清洗仅保留原文"的简短示例，帮助模型更稳定理解任务。
* 明确要求 `clean_text` 与 `segments` 必须保持一致，`segments` 必须来源于 `clean_text`，不得各写各的。
* 明确 `accept` 时 `clean_text` 不能为空，启用分段时 `segments` 不能为空。

**✂️ 修复：本地超上限分段不再过度合并**
* 重写本地 `_rebalance_segments_to_target()` 收敛策略。
* 当模型返回段数超出上限时，改为逐次合并最合适的相邻段，直到收敛到目标段数。
* 修复了极端情况下"设置最多 5 段，最终却被压成 2 段"的问题。

**… 优化：本地 fallback 支持省略号切分**
* 本地分段逻辑新增对 `……`、`…`、`...` 的识别。
* 在命中特殊拒绝原因、回退本地分段时，文本若主要依赖省略号形成语气停顿，也能得到更自然的切分结果。

---

### v1.2.5

**✂️ 调整：本地 fallback 分段策略收敛**
* 本地分段规则收敛为：**换行必切**，`。！？?!` 作为次级切点（按段长条件决定是否切分）。
* 移除逗号/顿号/冒号/分号等弱分隔符在本地 fallback 中的切分行为，降低碎片化。
* 保留代码块、URL、占位符与括号/引号上下文保护，避免误切关键内容。

**📏 优化：段数限制下的本地重平衡**
* 当用户开启"限制段数"且本地候选段数超上限时，不再采用"尾段全量合并"的旧策略。
* 改为按可见长度做目标段数重平衡合并，显著缓解"前段过碎、尾段过长"的头重脚轻问题。
* 未开启限制段数时，仍保持默认最多 12 段。

**🧹 内部整理：统一 fallback 路径**
* 本地 fallback 路径进一步统一，减少旧式弱兜底分支带来的行为差异。
* 在模型半失败场景下，尽量与模型全失败场景保持一致的本地分段体验。

---

### v1.2.4

**🪂 优化：特殊拒绝原因直切本地 fallback**
* 新增约定式特殊原因 `内容涉及不适合主题，拒绝处理`。
* 当后处理模型命中该原因时，插件会忽略模型返回的 `action / clean_text / segments`，直接回退到本地分段逻辑。
* 该逻辑同时覆盖初审与复审阶段，避免模型因拒绝处理而导致整条消息分段失效。

**🧼 优化：清洗目标描述更明确**
* 补强默认清洗规则文案，明确"清洗"不只是表面整理。
* 现在会明确要求：在不改变原意、不新增事实、不删减关键信息的前提下，把不适合直接发送的表达修整为适合直接发送的表达。
* 同时保留边界约束，禁止借清洗之名偷换语气、立场或事实内容。

**✂️ 优化：本地 fallback 分段更自然**
* 本地分段现在会尽量保护代码块、URL 与富媒体占位符，避免被粗暴切断。
* 增强对括号、引号等上下文的保护，减少语义还没说完就被切开的情况。
* 对过长段落增加二次细分，对过短段落增加自然合并，使主备模型不可用时的兜底结果更接近日常聊天节奏。

---

### v1.2.3

**🛡️ 新增：保真校验分级**
* 新增 `preserve_mode` 配置项，支持 `off / basic / strict` 三档。
* `off`：关闭保真校验，完全相信后处理模型结果。
* `basic`：校验 URL、代码块、数字等关键文本信息，不再强制校验富媒体占位符。
* `strict`：在 `basic` 基础上额外校验 `[[RP_COMP_x]]` 占位符，行为最保守。

**🧮 修复：占位符内部数字误报为数字缺失**
* 数字保真校验前会先剥离 `[[RP_COMP_x]]` 占位符。
* 不再把占位符编号里的数字（如 `1`）误判为真实正文数字缺失。
* 优化后相关日志更接近真实问题，不再出现 `数字串:1` 这类误导性报错。

**🧷 新增：尾部占位符自动补回**
* 当原始回复末尾带有连续 `[[RP_COMP_x]]` 占位符、而后处理模型只遗漏了这串尾部占位符时，插件会自动补回到最后一段末尾。
* 仅在"去占位符后的正文主体一致"时触发，避免误补到正文已明显改写的结果上。
* 该逻辑主要用于修复插件内部尾部富媒体占位符被后处理模型吞掉的问题，减少不必要的整条回退。

---

### v1.2.2

**🩹 修复：合法分段被误判为与 clean_text 不一致**
* 修正 `segments` 与 `clean_text` 的一致性比较口径。
* 现在比较时会忽略"分段换行"和"单条连续文本"的差异，不再把正常多段结果误判成异常。
* 解决了 `humanized` 分段明明生效，但最终又被错误回退成单段的问题。

**🧷 修复：占位符仅存在于 clean_text 时自动回填到 segments**
* 当模型输出的 `clean_text` 保留了 `[[RP_COMP_x]]` 占位符、但 `segments` 漏掉占位符时，插件会优先自动回填。
* 回填成功后继续保留原本分段，不再一律回退为单段。
* 主要用于兼容表情、图片、富媒体等非文本组件在后处理模型中的遗漏问题。

---

### v1.2.1

**🧷 修复：占位符差异导致表情/富媒体丢失风险**
* 补强 `segments` 与 `clean_text` 差异分支的处理策略。
* 针对"清洗文本保留占位符、分段文本丢占位符"的情况，新增自动回填逻辑。
* 降低后处理模型对非文本组件占位处理不稳定时造成的消息链缺损风险。

---

### v1.2.0

**🔁 新增：主模型 / 备用模型双模型后处理链路**
* 后处理支持配置主模型与备用模型。
* 主模型超时或失败时，自动切换备用模型继续执行。
* 主备模型共用统一超时配置，避免堆叠过多设置项。

**🧠 后处理全链路接入主备切换**
* 主备切换不只覆盖首轮审查，也覆盖打回重写与复审阶段。
* 任一阶段发生模型超时或调用失败，都会尝试切换备用模型继续处理。
* 提升在 provider 波动或模型卡顿场景下的稳定性。

**🪂 新增：主备模型均失败后的本地 fallback 分段**
* 当主模型与备用模型在某阶段均不可用时，插件会进入本地兜底处理。
* 若启用了分段功能，则执行本地轻量分段；若关闭分段，则回退原文单段发送。
* 本地 fallback 不做清洗、不做审查、不做打回，仅保证最低可用性。

**📋 日志增强**
* 新增备用模型切换日志。
* 新增主备模型均失败后进入本地处理的日志。
* 最终输出日志中增加处理模式字段，便于排查当前结果来自正常链路还是本地兜底。

---

### v1.1.0

**⏱️ 新增：后处理超时配置**
* 新增 `post_process_timeout_seconds` 配置项。
* 为后处理模型调用提供统一超时控制，降低因模型长时间卡住导致回复迟迟不发送的问题。

**📝 日志增强：记录模型处理耗时**
* 在最终输出日志中增加模型处理耗时。
* 便于观察后处理模型从输入到输出的大致耗时表现。

---

### v1.0.0

**✂️ 首次发布：以自然分段为核心的回复后处理器上线**

**1. ✂️ 回复后自然分段能力**
* 支持在主模型生成回复后进行二次处理，根据语义自然拆分为多段消息发送。
* 分段目标不是机械切句，而是尽量贴近即时聊天中的发送节奏。
* 支持分段偏好、段数范围控制与分段延迟配置。

**2. 🧼 自定义清洗能力**
* 支持通过独立 LLM 对回复做轻量清洗。
* 可按配置处理空行、符号、错别字、异体字、繁简体与非术语英文等问题。
* 清洗遵循保守原则，不以改写内容为目标。

**3. 🔎 自定义审查与打回重生成**
* 支持按用户自定义规则审查回复是否适合直接发送。
* 当回复存在明显问题且不适合直接放行时，可打回给当前对话模型重写。
* 重写结果会再次复审，避免直接把判退结果发出。

**4. 🧷 关键内容保真保护**
* 后处理流程会尽量保护 URL、数字、代码块、@提及与非文本组件占位符。
* 若检测到关键内容缺失或发生明显偏移，会优先回退原回复。
* 降低分段、清洗与重写过程中误伤正文的风险。

**5. 🖼️ 富媒体占位与引用保留支持**
* 支持在处理文本时对非文本组件做占位，并在最终消息中尽量恢复。
* 第一段可保留原始回复引用，兼容常见聊天场景。
* 若组件无法安全复制或恢复，会直接跳过本次后处理，避免消息链错位。

**6. 🔁 保守回退与异常兜底机制**
* 当后处理模型输出无法解析、复审失败、组件恢复失败或流程异常时，可回退原始回复。
* 同会话内使用串行锁，减少多条回复并发处理时的发送顺序混乱。
* 在保证可用性的前提下，优先避免将处理坏的内容发送给用户。
