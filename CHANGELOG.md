### v1.5.5

**📝 优化：合并句号/逗号去除配置项，统一模型与本地回退路径**

* 原有 `strip_segment_trailing_period` 与 `strip_local_fallback_period` 合并为单一配置项 `strip_segment_trailing_period`，同时作用于模型路径与本地回退路径。
* 新增逗号去除能力：分段末尾的单个逗号（，/,）现在也会被去除，使分段更干净自然。

---

### v1.5.4

**📝 新增：本地回退独立句号去除开关**

* 新增配置项 `strip_local_fallback_period`（默认开启），独立控制本地回退路径下分段末尾句号的去除行为。
* 原有 `strip_segment_trailing_period` 开关收窄为仅作用于模型处理路径，两个开关互不影响。
* 满足"本地回退时分段末尾不得出现单个句号"的差异化需求，省略号变式（。。。/。。）不受影响。

---

### v1.5.3

**📝 优化：本地回退分段方案语义感知增强**

* 新增弱标点（，、；：,;:）切分：当段长已达目标长度时，将弱标点作为切分点，避免长段粘连。阈值高于强标点，避免短段被过度切碎。
* 新增 emoji 切分：将 emoji 作为聊天的自然换气点，在段长达到阈值时于 emoji 后切分，使分段更贴近真人聊天节奏。
* 改进短段合并策略：以强标点 / emoji / 富媒体占位符结尾的短段视为自然边界，独立保留而不再强行并入前段，避免破坏语义完整性。

---

### v1.5.2

**🔧 优化：占位符不再出示给后处理 LLM**

* 富媒体占位符在发送给后处理 LLM 之前被剥离，LLM 返回后再补回，避免占位符被错误切分为单独分段或与标点粘连。
* 打回重写流程同步接入占位符保护，重写文本返回前自动补回尾部占位符。
* 清理默认提示词中占位符相关示例。
* 修复版本号不一致的历史遗留问题。

---

### v1.5.1

**📝 优化：占位符不得单独成段**

* 修复模型将末尾富媒体占位符单独切为一个分段的问题。
* 新增约束：占位符不得单独成为一个分段，必须与相邻文本同段保留。
* 更新示例，强调占位符附在前段末尾而非独立成段。

---

### v1.5.0

**🛡️ 修复：多项稳定性问题**

* 修复分段发送失败时用户收到重复内容：前置分段已发出后若发送异常，改为继续发送最后一段，不再回退完整原文。
* 修复后处理模型返回非字符串类型时可能崩溃的问题。
* 修复长时间运行后会话锁可能无限增长的问题，现已自动清理过期锁。
* 修复不匹配的括号导致本地分段失效的问题。

---

### v1.4.9

**📝 优化：拟人化分段提示词重构**

* 拟人化分段提示词重构：以"边想边发的节奏"为方向、"换气点"为核心比喻、"不为整齐而合并"为反约束。
* 降低本地回退拟人化目标段长：18 字 → 10 字。
* 降低本地短段合并地板阈值：6 字 → 2 字。

---

### v1.4.8

**🧹 优化：精简白名单检测日志输出**

* 白名单检测日志从 INFO 级别降为 DEBUG 级别，未匹配到跳过规则时不再刷屏。
* 匹配到跳过规则时仅输出一行简洁日志。
* 需要调试时可开启 `debug_log` 配置项查看详细信息。

---

### v1.4.7

**🐛 修复：白名单过滤失效问题**

* 修复 `skip_command_prefix` 配置项不生效的问题：指令前缀检测改为读取原始消息文本，避免被框架唤醒阶段剥离。

---

### v1.4.6

**🛡️ 新增：白名单指令过滤与纯占位符检测**

* 新增 `skip_command_prefix` 配置项（默认开启），用户发送以 `/` 开头的消息时跳过本插件处理。
* 新增 `skip_patterns` 配置项（正则表达式列表），用户原始输入匹配任一规则时跳过处理。
* 新增纯占位符检测：纯图片/纯文件等无文本回复跳过全部处理，避免被 LLM 错误添加内容。

---

### v1.4.5

**🐛 修复：末尾含占位符时分段句号清理失效**

* 修复分段末尾为富媒体占位符时，占位符前的句号未被清理的问题。

---

### v1.4.4

**📝 优化：特殊拒绝标准收紧**

* 特殊拒绝判断从模糊的"不适合主题"改为明确列举四类：露骨色情、政治敏感、涉及儿童、自残/暴力/违禁品。
* 新增"轻微擦边、暧昧暗示、日常调侃必须放行"的约束，避免过度拒绝。

**🐛 修复：单段输出末尾句号未清理**

* 无论走哪条代码路径，单段输出的末尾句号现在都会被正确清理。

**✨ 新增：字数限制功能**

* 新增 `enable_max_process_length` 配置项（默认开启）。
* 新增 `max_process_length` 配置项（默认 500），纯文本字数超过此值时跳过后处理，原样发送。

---

### v1.4.3

**📝 优化：提示词精简与增强**

* 精简提示词中的重复文本，让用户自定义规则更突出。
* 新增 3 个 few-shot 示例：短文本无需分段、包含占位符、包含换行。
* 优化 reason 字段格式要求，给出具体示例。

---

### v1.4.2

**✨ 新增：处理所有回复配置项**

* 新增 `process_all_replies` 配置项（默认关闭），开启后会将插件工具调用等非 LLM 回复也纳入分段与清洗范围。
* 关闭时行为不变，仅处理 LLM 直接生成的回复。

---

### v1.4.1

**✨ 新增：分段末尾句号去除配置项**

* 新增 `strip_segment_trailing_period` 配置项（默认开启），控制每个分段是否不得以单个句号结尾。
* "分段末尾去句号"从清洗阶段迁移到分段阶段：清洗阶段保留所有句号，分段阶段负责去除末尾单个句号。
* 省略号变式（。。。/。。）不受影响。
* 更新示例，反映新设计。

---

### v1.4.0

**🐛 修复：最后一段消息发送延迟过高**

* 移除最后一段前缀发送后的多余延迟等待，修复最后一段（尤其是语音）发出明显偏慢的问题。
* 现在最后一段前缀发送后直接交由 AstrBot 管道处理，到达时间更自然。

---

### v1.3.9

**📝 更新：插件描述与配置提示词**

* 插件描述改为"LLM 驱动的回复分段器"，突出差异化。
* 精简冗余 hint，补充清洗、判别、过渡语等条目的说明。

---

### v1.3.8

**🛡️ 修复：保真校验相关问题**

* 修复复审阶段保真校验误判：URL/数字等合理变化不再被误判为"内容丢失"。
* 修复数字 `1.5` 与 `15` 被误判为不同的假阳性问题。
* 保真校验失败时回退本地分段而非单段原文。

---

### v1.3.7

**🛡️ 修复：JSON 解析与合并评分**

* JSON 解析失败时走本地分段回退。
* 弱标点开头段落合并评分改为减分，强标点开头保持正向加分。

**🧹 移除冗余配置项**

* 移除 `fallback_to_original_on_error`、`enable_segments_consistency_check`、`enable_retry_notice`。

**📝 优化：提示词与清洗规则**

* 移除用户消息传入后处理上下文，减少注意力干扰。
* INFO 日志截断为 500 字符。

---

### v1.3.6

**🛡️ 修复：配置与代码默认值不一致**

* `judge_prompt_template` 和 `retry_prompt_template` 配置默认值对齐代码默认。
* 分段延迟模式回退默认值从 `fixed` 改为 `linear`。
* 修复 `_judge_reply()` 未传 `user_message` 的问题。

**📝 配置 hint 补充完整占位符列表**

---

### v1.3.5

**🛡️ 修复：分段后占位符仍丢失的兜底补回**

* 新增占位符最终兜底检查：分段后若仍有占位符缺失，直接补回最后一段。
* 修复模型分段输出与清洗文本存在微小差异时占位符无法传递的问题。
* 首次分段路径与重试分段路径均已接入该兜底逻辑。

---

### v1.3.4

**🛡️ 修复：clean_text 为空时回退原文**

* 模型返回空 `clean_text` 时自动回退原文，保证后处理结果始终有实际内容。

**🧷 修复：重复占位符丢失检测不准确**

* 修复原文存在重复占位符时，清洗后丢失的占位符无法被识别的问题。现在按出现次数逐一比对，缺失几个补回几个。

**📝 文档更新：README 与现版本逻辑对齐**

* README 删除已过时或不准确的描述，统一按当前实现说明插件行为。
* 同步更新 `preserve_mode` 说明。

**🧹 优化：打回规则接入重写提示词**

* 打回规则现在在重写路径中真正生效。
* `force_local_fallback_reason` 不再暴露给用户配置。

---

### v1.3.3

**🧹 清除遗留：插件标识统一**

* 移除所有 `ReplyPolisher` 旧标识，统一改为 `PostSplitter`。

**📝 优化：默认模板示例增强**

* 示例新增场景说明：补全标点仅适用于超长连续文本；省略号变式与分段末尾句号的区别处理。

---

### v1.3.2

**📝 优化：非调试模式下的日志输出完善**

* 非调试模式下现在会完整输出原文本、段数、总长度、模型耗时、处理模式和分段结果。

**✅ 修复：分段一致性校验开关逻辑**

* 关闭一致性校验开关时，直接信任模型分段结果，不触发回退。

---

### v1.3.1

**🔒 占位符检查永久开启**

* 清洗后立即检测占位符是否丢失，丢失则自动补回，不受任何开关影响。

**📝 提示词动态生成**

* 提示词步骤现在根据开关动态生成：未开启的功能不再传入对应步骤，避免扰乱模型注意力。
* 段数限制仅在开启分段功能时显示。

**🛡️ 加强提示词引导**

* 严格禁止区域新增内容完整性约束，降低漏段概率。

---

### v1.3.0

**🔧 配置默认值调整**

* `enable_clean` 默认值改为 `true`，清洗功能默认启用。
* `enable_segment_count_range` 默认值改为 `true`，段数限制默认启用。
* `min_segments` 默认值改为 `2`，最小段数默认为 2。

**🧼 清洗规则新增**

* `clean_prompt_template` 默认值新增规则：清洗掉分段末尾的句号。

**✂️ 分段一致性校验可配置化**

* 新增 `enable_segments_consistency_check` 配置项，默认 `false`。
* 允许关闭 `segments` 与 `clean_text` 的一致性校验，获得更自由的分段结果。

**🧹 移除未使用的配置项**

* 移除 `enable_clean_content_check`、`protected_tone_words`。

**📝 精简提示词与输出格式**

* 输出格式简化为：`{"action", "reason", "clean_text", "segments"}`，减少模型需要填充的冗余字段。

---

### v1.2.6

**🧠 优化：后处理主提示词改为严格流水线执行**

* 将默认后处理提示词明确拆分为"特殊拒绝判断 → 是否打回 → 清洗 → 分段"四步。
* 明确 `accept` 表示"已经完成当前已启用的后处理"，降低模型只做审查不执行处理的概率。

**📝 优化：reason 语义收紧**

* 强化 `reason` 的输出约束，要求描述实际处理结果，禁止空洞套话。

**✂️ 修复：本地超上限分段不再过度合并**

* 当模型返回段数超出上限时，改为逐次合并最合适的相邻段，直到收敛到目标段数。
* 修复了极端情况下"设置最多 5 段，最终却被压成 2 段"的问题。

**… 优化：本地 fallback 支持省略号切分**

* 本地分段逻辑新增对 `……`、`…`、`...` 的识别。

---

### v1.2.5

**✂️ 调整：本地 fallback 分段策略收敛**

* 本地分段规则收敛为：换行必切，`。！？?!` 作为次级切点（按段长条件决定是否切分）。
* 移除逗号/顿号/冒号/分号等弱分隔符在本地 fallback 中的切分行为，降低碎片化。

**📏 优化：段数限制下的本地重平衡**

* 当用户开启"限制段数"且本地候选段数超上限时，不再采用"尾段全量合并"的旧策略。
* 改为按可见长度做目标段数重平衡合并，显著缓解"前段过碎、尾段过长"的头重脚轻问题。
* 未开启限制段数时，仍保持默认最多 12 段。

**🧹 内部整理：统一 fallback 路径**

* 本地 fallback 路径进一步统一，减少旧式弱兜底分支带来的行为差异。

---

### v1.2.4

**🪂 优化：特殊拒绝原因直切本地 fallback**

* 新增约定式特殊原因 `内容涉及不适合主题，拒绝处理`。
* 当后处理模型命中该原因时，插件会忽略模型返回结果，直接回退到本地分段逻辑。
* 该逻辑同时覆盖初审与复审阶段。

**🧼 优化：清洗目标描述更明确**

* 明确要求：在不改变原意、不新增事实、不删减关键信息的前提下，把不适合直接发送的表达修整为适合直接发送的表达。

**✂️ 优化：本地 fallback 分段更自然**

* 本地分段现在会尽量保护代码块、URL 与富媒体占位符，避免被粗暴切断。
* 对过长段落增加二次细分，对过短段落增加自然合并。

---

### v1.2.3

**🛡️ 新增：保真校验分级**

* 新增 `preserve_mode` 配置项，支持 `off / basic / strict` 三档。
* `off`：关闭保真校验，完全相信后处理模型结果。
* `basic`：校验 URL、代码块、数字等关键文本信息，不再强制校验富媒体占位符。
* `strict`：在 `basic` 基础上额外校验 `[[RP_COMP_x]]` 占位符，行为最保守。

**🧮 修复：占位符内部数字误报为数字缺失**

* 数字保真校验前会先剥离 `[[RP_COMP_x]]` 占位符，不再把占位符编号里的数字误判为真实正文数字缺失。

**🧷 新增：尾部占位符自动补回**

* 当原始回复末尾带有连续 `[[RP_COMP_x]]` 占位符、而后处理模型只遗漏了这串尾部占位符时，插件会自动补回到最后一段末尾。

---

### v1.2.2

**🩹 修复：合法分段被误判为与 clean_text 不一致**

* 修正 `segments` 与 `clean_text` 的一致性比较口径，不再把正常多段结果误判成异常。
* 解决了 `humanized` 分段明明生效，但最终又被错误回退成单段的问题。

**🧷 修复：占位符仅存在于 clean_text 时自动回填到 segments**

* 当模型输出的 `clean_text` 保留了占位符、但 `segments` 漏掉占位符时，插件会优先自动回填，不再一律回退为单段。

---

### v1.2.1

**🧷 修复：占位符差异导致表情/富媒体丢失风险**

* 针对"清洗文本保留占位符、分段文本丢占位符"的情况，新增自动回填逻辑。
* 降低后处理模型对非文本组件占位处理不稳定时造成的消息链缺损风险。

---

### v1.2.0

**🔁 新增：主模型 / 备用模型双模型后处理链路**

* 后处理支持配置主模型与备用模型。
* 主模型超时或失败时，自动切换备用模型继续执行。
* 主备模型共用统一超时配置。

**🧠 后处理全链路接入主备切换**

* 主备切换不只覆盖首轮审查，也覆盖打回重写与复审阶段。

**🪂 新增：主备模型均失败后的本地 fallback 分段**

* 当主模型与备用模型在某阶段均不可用时，插件会进入本地兜底处理。
* 若启用了分段功能，则执行本地轻量分段；若关闭分段，则回退原文单段发送。
* 本地 fallback 不做清洗、不做审查、不做打回，仅保证最低可用性。

**📋 日志增强**

* 新增备用模型切换日志。
* 最终输出日志中增加处理模式字段，便于排查当前结果来自正常链路还是本地兜底。

---

### v1.1.0

**⏱️ 新增：后处理超时配置**

* 新增 `post_process_timeout_seconds` 配置项，为后处理模型调用提供统一超时控制。
* 在最终输出日志中增加模型处理耗时。

---

### v1.0.0

**✂️ 首次发布：以自然分段为核心的回复后处理器上线**

**1. ✂️ 回复后自然分段能力**

* 支持在主模型生成回复后进行二次处理，根据语义自然拆分为多段消息发送。
* 支持分段偏好、段数范围控制与分段延迟配置。

**2. 🧼 自定义清洗能力**

* 支持通过独立 LLM 对回复做轻量清洗。
* 可按配置处理空行、符号、错别字、异体字、繁简体与非术语英文等问题。

**3. 🔎 自定义审查与打回重生成**

* 支持按用户自定义规则审查回复是否适合直接发送。
* 当回复存在明显问题且不适合直接放行时，可打回给当前对话模型重写。
* 重写结果会再次复审，避免直接把判退结果发出。

**4. 🧷 关键内容保真保护**

* 后处理流程会尽量保护 URL、数字、代码块、@提及与非文本组件占位符。
* 若检测到关键内容缺失或发生明显偏移，会优先回退原回复。

**5. 🖼️ 富媒体占位与引用保留支持**

* 支持在处理文本时对非文本组件做占位，并在最终消息中尽量恢复。
* 第一段可保留原始回复引用，兼容常见聊天场景。
* 若组件无法安全复制或恢复，会直接跳过本次后处理，避免消息链错位。

**6. 🔁 保守回退与异常兜底机制**

* 当后处理模型输出无法解析、复审失败、组件恢复失败或流程异常时，可回退原始回复。
* 同会话内使用串行锁，减少多条回复并发处理时的发送顺序混乱。
